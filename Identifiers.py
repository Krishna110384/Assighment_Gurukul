# Identifiers

#123total=1 # not a identifier as starting with no as first
total123 = 1 # Identifier
java2share = 1 # Identifier
#ca$h = 1 #Not a identifier as special case is used
_abc_abc_ =1 # Identifier
Def = 1 #Identifier
#if =1 # Not a identifer as if is used as reserved words

import keyword
print(keyword.kwlist)
